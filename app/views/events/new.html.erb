<h1>New event</h1>

<%= error_messages_for :event, :image %>

<% form_for([@box, @event], new_box_event_path(@box), :html => {:multipart => true}) do |f| %>

	<%= render :partial => "form", :locals => {:f => f} %>

<%= f.submit "Submit" %>
<% end %>