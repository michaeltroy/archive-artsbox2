<h1>Editing event</h1>

<%= error_messages_for :event %>

<% form_for([@box, @event], box_event_path(@box), :html => {:multipart => true}) do |f| %>

	<%= render :partial => "form", :locals => {:f => f} %>

<%= f.submit "Submit" %>
<% end %>
