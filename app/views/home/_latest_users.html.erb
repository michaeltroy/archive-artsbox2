
	<div id="latest_users">
		<h2>New users</h2>
		<!-- latest users -->
		<% for user in @latest_users %>
		<span class="avatar_small">
		<%= link_to "#{image_tag user.avatar.url(:tiny)}", permalink_path(user) %>
		</span>
		<% end %>
	</div>