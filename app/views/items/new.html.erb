<h1>New item</h1>

<% form_for([@user, @item], new_user_item_path(@user)) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form", :locals => {:f => f} %> <br />
  <%= f.submit "List item" %>
<% end %>